services:
  - type: web
    name: jewelry-odoo
    env: python
    plan: free
    region: oregon
    branch: main
    buildCommand: chmod +x bulletproof_build.sh && ./bulletproof_build.sh
    startCommand: python odoo-bin -c odoo.conf --workers=1 --without-demo=all
